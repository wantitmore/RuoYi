<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.web.controller.biz.meeting.mapper.MeetingLogMapper">
    
    <resultMap type="MeetingLog" id="MeetingLogResult">
        <result property="id"    column="id"    />
        <result property="meetingDate"    column="meeting_date"    />
        <result property="meetingContent"    column="meeting_content"    />
        <result property="deptId"    column="dept_id"    />
        <result property="createBy"    column="create_by"    />
        <result property="createTime"    column="create_time"    />
        <result property="updateBy"    column="update_by"    />
        <result property="updateTime"    column="update_time"    />
    </resultMap>

    <sql id="selectMeetingLogVo">
        select id, meeting_date, meeting_content, dept_id, create_by, create_time, update_by, update_time from meeting_log
    </sql>

    <select id="selectMeetingLogList" parameterType="MeetingLog" resultMap="MeetingLogResult">
        <include refid="selectMeetingLogVo"/>
        <where>  
            <if test="meetingDate != null "> and meeting_date = #{meetingDate}</if>
            <if test="meetingContent != null  and meetingContent != ''"> and meeting_content = #{meetingContent}</if>
            <if test="deptId != null "> and dept_id = #{deptId}</if>
        </where>
    </select>
    
    <select id="selectMeetingLogById" parameterType="Long" resultMap="MeetingLogResult">
        <include refid="selectMeetingLogVo"/>
        where id = #{id}
    </select>

    <insert id="insertMeetingLog" parameterType="MeetingLog" useGeneratedKeys="true" keyProperty="id">
        insert into meeting_log
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="meetingDate != null">meeting_date,</if>
            <if test="meetingContent != null">meeting_content,</if>
            <if test="deptId != null">dept_id,</if>
            <if test="createBy != null">create_by,</if>
            <if test="createTime != null">create_time,</if>
            <if test="updateBy != null">update_by,</if>
            <if test="updateTime != null">update_time,</if>
         </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="meetingDate != null">#{meetingDate},</if>
            <if test="meetingContent != null">#{meetingContent},</if>
            <if test="deptId != null">#{deptId},</if>
            <if test="createBy != null">#{createBy},</if>
            <if test="createTime != null">#{createTime},</if>
            <if test="updateBy != null">#{updateBy},</if>
            <if test="updateTime != null">#{updateTime},</if>
         </trim>
    </insert>

    <update id="updateMeetingLog" parameterType="MeetingLog">
        update meeting_log
        <trim prefix="SET" suffixOverrides=",">
            <if test="meetingDate != null">meeting_date = #{meetingDate},</if>
            <if test="meetingContent != null">meeting_content = #{meetingContent},</if>
            <if test="deptId != null">dept_id = #{deptId},</if>
            <if test="createBy != null">create_by = #{createBy},</if>
            <if test="createTime != null">create_time = #{createTime},</if>
            <if test="updateBy != null">update_by = #{updateBy},</if>
            <if test="updateTime != null">update_time = #{updateTime},</if>
        </trim>
        where id = #{id}
    </update>

    <delete id="deleteMeetingLogById" parameterType="Long">
        delete from meeting_log where id = #{id}
    </delete>

    <delete id="deleteMeetingLogByIds" parameterType="String">
        delete from meeting_log where id in 
        <foreach item="id" collection="array" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>

</mapper>